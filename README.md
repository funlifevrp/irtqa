# نظام إدارة الحلقات - الإصدار الجديد 3.0

## نظرة عامة

هذا هو النظام الجديد لإدارة الحلقات، تم إعادة بناؤه من الصفر ببنية كودية متقدمة وخالية من الأخطاء. يتميز النظام بالأمان العالي، الأداء المحسن، والواجهة العصرية.

## المميزات الجديدة

### 🔒 الأمان المتقدم
- حماية CSRF شاملة لجميع النماذج
- تشفير كلمات المرور باستخدام أحدث المعايير
- حماية من هجمات SQL Injection
- نظام Rate Limiting لمنع هجمات Brute Force
- تسجيل شامل لجميع العمليات في Audit Log

### 🏗️ البنية المعمارية الجديدة
- نظام Autoloader للتحميل التلقائي للكلاسات
- فصل واضح بين طبقات النظام (MVC Pattern)
- معالجة شاملة للأخطاء مع نظام Logging متقدم
- طبقة قاعدة بيانات محسنة مع Connection Pooling
- نظام Security Layer متكامل

### 🎨 التصميم والواجهة
- تصميم عصري ومتجاوب (Responsive Design)
- دعم كامل للأجهزة المحمولة
- واجهة مستخدم بديهية وسهلة الاستخدام
- رسوم بيانية تفاعلية للإحصائيات
- تأثيرات بصرية جذابة

### ⚡ الأداء المحسن
- استعلامات قاعدة بيانات محسنة
- فهارس ذكية لتسريع البحث
- تحميل تدريجي للبيانات الكبيرة
- ذاكرة تخزين مؤقت للاستعلامات المتكررة

## متطلبات النظام

### متطلبات الخادم
- PHP 8.0 أو أحدث
- MySQL 8.0 أو أحدث (أو MariaDB 10.4+)
- Apache 2.4 أو Nginx
- الذاكرة: 512 ميجابايت على الأقل
- مساحة القرص: 100 ميجابايت على الأقل

### الإضافات المطلوبة لـ PHP
- php-mysql
- php-mbstring
- php-json
- php-session
- php-curl
- php-gd (اختياري للصور)

## بيانات تسجيل الدخول الافتراضية

### المبرمج الرئيسي
- **اسم المستخدم:** `sle`
- **كلمة المرور:** `suliman2025`
- **الصلاحيات:** جميع الصلاحيات

## هيكل المشروع

```
halqat_new/
├── config/                 # ملفات الإعدادات
│   └── config.php         # الإعدادات الرئيسية
├── includes/              # الملفات الأساسية
│   └── core/             # الكلاسات الأساسية
│       ├── Autoloader.php # نظام التحميل التلقائي
│       ├── Database.php   # طبقة قاعدة البيانات
│       ├── Auth.php       # نظام المصادقة
│       ├── Security.php   # طبقة الأمان
│       └── Logger.php     # نظام تسجيل الأحداث
├── app/                   # تطبيق النظام
│   └── models/           # نماذج البيانات
│       └── User.php      # نموذج المستخدم
├── logs/                  # سجلات النظام
├── storage/              # ملفات التخزين
├── public/               # الملفات العامة
│   └── assets/          # الأصول (CSS, JS, Images)
├── database.sql          # قاعدة البيانات
├── login.php            # صفحة تسجيل الدخول
├── dashboard.php        # لوحة التحكم
├── logout.php           # تسجيل الخروج
├── users.php            # إدارة المستخدمين
└── README.md            # هذا الملف
```

## الصفحات المتوفرة

### ✅ الصفحات المكتملة
1. **صفحة تسجيل الدخول** (`login.php`)
   - تصميم عصري ومتجاوب
   - حماية من هجمات Brute Force
   - تسجيل محاولات الدخول

2. **لوحة التحكم** (`dashboard.php`)
   - إحصائيات شاملة ومباشرة
   - رسوم بيانية تفاعلية
   - عرض الأنشطة الأخيرة
   - إجراءات سريعة

3. **إدارة المستخدمين** (`users.php`)
   - إضافة وتعديل المستخدمين
   - فلاتر بحث متقدمة
   - إدارة الأدوار والصلاحيات
   - تسجيل جميع العمليات

4. **تسجيل الخروج** (`logout.php`)
   - تسجيل آمن للخروج
   - تصميم جذاب مع عد تنازلي
   - تسجيل العملية في السجلات

### 🔄 الصفحات قيد التطوير
- إدارة الطلاب
- إدارة الحلقات
- نظام الحضور والغياب
- إدارة الدرجات
- التقارير والإحصائيات
- إعدادات النظام

## الأدوار والصلاحيات

### 👨‍💻 المبرمج
- جميع الصلاحيات بدون استثناء
- إدارة النظام والإعدادات
- عرض السجلات والتقارير
- إدارة المستخدمين والأدوار

### 👨‍💼 المشرف
- إدارة الحلقات والطلاب
- إدارة الحضور والغياب
- عرض التقارير
- إدارة المعلمين

### 👨‍🏫 المعلم
- إدارة حلقاته فقط
- تسجيل الحضور والغياب
- إدخال الدرجات
- عرض تقارير طلابه

## قاعدة البيانات

### الجداول الرئيسية
- `users` - المستخدمين
- `students` - الطلاب
- `halaqat` - الحلقات
- `attendance` - الحضور والغياب
- `grades` - الدرجات
- `courses` - المقررات
- `audit_log` - سجل العمليات
- `system_settings` - إعدادات النظام

### المميزات
- علاقات خارجية للحفاظ على سلامة البيانات
- فهارس محسنة للأداء العالي
- تشفير البيانات الحساسة
- نسخ احتياطية تلقائية

## نظام السجلات

### أنواع السجلات
- **Info:** معلومات عامة
- **Warning:** تحذيرات
- **Error:** أخطاء
- **Critical:** أخطاء حرجة
- **Debug:** معلومات التطوير

### مواقع السجلات
- `logs/system.log` - سجل النظام العام
- `logs/auth.log` - سجل المصادقة
- `logs/database.log` - سجل قاعدة البيانات
- `logs/security.log` - سجل الأمان

## الأمان والحماية

### التدابير الأمنية
1. **تشفير كلمات المرور:** باستخدام `password_hash()` مع `PASSWORD_DEFAULT`
2. **حماية CSRF:** رموز أمان لجميع النماذج
3. **تنظيف المدخلات:** تطهير جميع البيانات الواردة
4. **Rate Limiting:** منع هجمات Brute Force
5. **Session Security:** إعدادات جلسة آمنة
6. **SQL Injection Protection:** استعلامات محضرة
7. **XSS Protection:** تنظيف المخرجات

### إعدادات الأمان
- انتهاء الجلسة بعد 30 دقيقة من عدم النشاط
- قفل الحساب بعد 5 محاولات دخول خاطئة
- مدة القفل: 15 دقيقة
- تسجيل جميع العمليات الحساسة

## استكشاف الأخطاء وإصلاحها

### المشاكل الشائعة وحلولها

#### 1. خطأ الاتصال بقاعدة البيانات
```
الخطأ: Database connection failed
الحل: تحقق من إعدادات قاعدة البيانات في config/config.php
```

#### 2. صفحة بيضاء أو خطأ 500
```
الحل: 
1. تحقق من سجلات الأخطاء في logs/
2. تأكد من صلاحيات الملفات والمجلدات
3. تحقق من إعدادات PHP
```

#### 3. مشكلة في الصلاحيات
```
الحل:
sudo chown -R www-data:www-data /path/to/halqat
sudo find /path/to/halqat -type d -exec chmod 755 {} \;
sudo find /path/to/halqat -type f -exec chmod 644 {} \;
```

#### 4. مشكلة في تسجيل الدخول
```
الحل:
1. تحقق من بيانات المستخدم في قاعدة البيانات
2. تأكد من تشغيل خدمة MySQL
3. راجع سجل المصادقة في logs/auth.log
```

### أوامر مفيدة للصيانة

#### إعادة تعيين كلمة مرور المبرمج
```sql
UPDATE users SET password = '$2y$10$92IXUNpkjO0rOQ5byMi.Ye4oKoEa3Ro9llC/.og/at2.uheWG/igi' 
WHERE username = 'sle';
-- كلمة المرور الجديدة: password
```

#### تنظيف السجلات القديمة
```sql
DELETE FROM audit_log WHERE created_at < DATE_SUB(NOW(), INTERVAL 30 DAY);
```

#### فحص حالة النظام
```bash
# فحص خدمة MySQL
sudo systemctl status mysql

# فحص خدمة Apache
sudo systemctl status apache2

# فحص سجلات الأخطاء
tail -f /var/log/apache2/error.log
```

## التطوير والتخصيص

### إضافة صفحة جديدة
1. إنشاء ملف PHP جديد
2. تضمين `config/config.php`
3. استخدام `Auth::requireLogin()` للحماية
4. اتباع نمط التصميم الموحد

### إضافة صلاحية جديدة
1. تحديث `USER_ROLES` في `config/config.php`
2. استخدام `Auth::requirePermission('permission_name')`
3. تحديث واجهة إدارة المستخدمين

### إضافة جدول جديد
1. إنشاء الجدول في قاعدة البيانات
2. إنشاء نموذج في `app/models/`
3. إضافة العمليات المطلوبة
4. تحديث واجهة المستخدم

## الدعم والمساعدة

### معلومات الاتصال
- **المطور:** فريق تطوير نظام الحلقات
- **الإصدار:** 3.0.0
- **تاريخ الإصدار:** أغسطس 2025

### الموارد المفيدة
- [دليل PHP الرسمي](https://www.php.net/manual/)
- [دليل MySQL الرسمي](https://dev.mysql.com/doc/)
- [Bootstrap Documentation](https://getbootstrap.com/docs/)

---

**ملاحظة:** هذا النظام تم تطويره خصيصاً لإدارة الحلقات التعليمية ويمكن تخصيصه حسب احتياجاتك الخاصة.

